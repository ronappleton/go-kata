package kata

// TestCase — a single test to run. Implement as specified in README.md.
type TestCase struct {
	Name string
	Run  func() error
}

// RunTests — TODO: implement as specified in README.md
//
// Runs each test with at most workers concurrent. workers < 1 means
// unlimited. Returns (passed, failed).
func RunTests(tests []TestCase, workers int) (passed int, failed int) {
	// TODO: your implementation here
	return 0, 0
}
